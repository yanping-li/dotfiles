# Default settings for all hosts
Host *
    IgnoreUnknown UseKeychain
    ForwardAgent yes
    AddKeysToAgent yes
    ServerAliveInterval 60
    ServerAliveCountMax 10
    # Allow legacy RSA host keys (disabled by default in newer OpenSSH)
    HostKeyAlgorithms +ssh-rsa

# macOS-only: store passphrases in Keychain
Match host * exec "test $(uname) = Darwin"
    UseKeychain yes

# Hosts are needed for vscode remote ssh
# Container Dev Servers (U22) - added from engweb /stable/
Host volga yukon zambezi aldan tunguska ural salween mekong paraguay yellow niagra murray mackenzie
    User lexli
